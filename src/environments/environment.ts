export const environment = {
  production: false,
  apiUrl: 'https://d223juhqt63axh.cloudfront.net/api/v1'   // relative URL — works with the proxy we configure next  // relative URL — works with the proxy we configure next
  // apiUrl: 'http://localhost:8080/api/v1'
};